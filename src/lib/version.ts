/**
 * Bumped on every set of changes shipped, so you can confirm at a glance
 * whether the live site has actually picked up the latest deploy. Shown in
 * the sidebar footer.
 */
export const SITE_VERSION = 'v18';
export const SITE_VERSION_DATE = '2026-09-21';
